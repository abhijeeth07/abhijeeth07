#include <stdio.h>
 int main ()
 {
 	int units,amt;
 	printf(" units consumed are");
 	scanf("%d",&units);
 	if(units>=0&&units<=50)
 	 {
 	   amt=units*1;	
	  }
 	else if (units<=100)
 	{
 		amt=25+((unit-50)*2);
	 }
	 else if(units<=200)
	 {
	 	amt=100+((unit-150)*3);
	 }
	 else if(units<=350)
	 {
	 	amt=
	 }
 }
