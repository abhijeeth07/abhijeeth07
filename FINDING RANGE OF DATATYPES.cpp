#include<stdio.h>
#include<limits.h>
main()
{

	printf("the maximum range=%d",INT_MAX);
    printf("the minimum range=%d",INT_MIN);
    printf("the max range=%ld",LONG_MAX);
    printf("the min range=%ld",LONG_MIN);
    
    
}
